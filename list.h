/*
#ifndef LIST_H_INCLUDED
#define LIST_H_INCLUDED
#include "datastruct.h"

int appendInList(TeamList *);
int insertInList(TeamList *);
TeamList *removeFromList(int );

typedef enum {forward, backward} TDirection;
void printList(TDirection );

#endif
*/
